server:
  port: 80

feign:
  hystrix:
    enabled: true #开启服务降级，在服务器不可用时也会获得提示信息而不会挂起耗死服务器；

# 从eureka中去找地址
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka,http://eureka7003.com:7003/eureka/

